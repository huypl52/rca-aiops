# `playbooks/` — Qdrant playbook store + floor registry

Trách nhiệm: Qdrant playbook store (FR-3) + floor registry declarative YAML (AD-12).

**Trạng thái:** DATA DIR (Story 0.1) — placeholder. Implement thực ở Epic 3/4.
