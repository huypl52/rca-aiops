# observability/manifests/10-prometheus.yaml — Prometheus (metric read target) + scrape rules.
#
# READ TARGET (Story 7.2 LOCK §1): Prometheus scrapes the 5 demo services' /metrics (the
# 7.1 render_prometheus exposition) via kubernetes_sd in the `demo` namespace, and the RCA
# backend (deployed Story 7.3). The agent's read-only adapters (2.2) query this Prometheus
# at runtime; the adapter read path is INHERITED read-only (AC3, deferred real-transport
# wiring to 5-A1). Alerting rules emit alerts whose `labels.alertname`/`labels.scenario`
# map to §3.7 canonicals so the Alertmanager -> ingest path produces valid 1.1 envelopes.
#
# Component version deferred to environment (Story 7.2 LOCK — DEFERRED); image tag is a
# known-good recent Prometheus, swappable.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: observability
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s
    rule_files: [/etc/prometheus/rules.yml]
    alerting:
      alertmanagers:
        - static_configs:
            - targets: [alertmanager.observability.svc.cluster.local:9093]
    scrape_configs:
      # The 5 demo SUT services — /metrics comes from the 7.1 deterministic render_prometheus
      # (each FastAPI service exposes GET /metrics, demo/app/factory.py). The demo Services are
      # ClusterIP port 80 → containerPort 8080..8084 (demo/k8s/20-services.yaml). Carry-forward
      # fix (7.2 had a single-host `replacement: demo` relabel with no port): per-service static
      # targets name each demo Service by DNS. The metric EXPOSITION already carries the `service`
      # label (render_prometheus labels service/operation/target), so no static `service` label is
      # added (avoids an honor_labels conflict) — the rules' `sum by (service)` resolves from the
      # scraped series, not the target.
      - job_name: demo-services
        metrics_path: /metrics
        static_configs:
          - targets:
              - api-gateway.demo.svc.cluster.local:80
              - user.demo.svc.cluster.local:80
              - order.demo.svc.cluster.local:80
              - inventory.demo.svc.cluster.local:80
              - payment.demo.svc.cluster.local:80
      - job_name: rca-backend
        static_configs:
          - targets: [rca-backend.rca.svc.cluster.local:8000]  # deployed Story 7.3
            labels: { service: rca-backend }
  rules.yml: |
    groups:
      - name: rca-demo-alerts
        rules:
          # alertname == §3.7 canonical OR carries labels.scenario -> normalizes cleanly.
          - alert: DependencyTimeout
            expr: sum by (service) (demo_upstream_errors_total) > 0
            for: 1m
            labels: { severity: critical, scenario: dependency_timeout }
            annotations: { summary: "upstream dependency timing out", description: "demo_upstream_errors_total > 0" }
          - alert: PaymentFailureHigh
            expr: sum by (service) (demo_upstream_errors_total{service="order"}) > 0
            for: 1m
            labels: { severity: critical, scenario: payment_failure }
            annotations: { summary: "payment charge failures", description: "order payment upstream errors" }
          - alert: InventoryReserveFailureHigh
            expr: sum by (service) (demo_upstream_errors_total{service="order"}) > 0
            for: 1m
            labels: { severity: warning, scenario: inventory_reserve_failure }
            annotations: { summary: "inventory reserve failures", description: "order inventory upstream errors" }
          - alert: DownstreamLatencyHigh
            expr: sum by (service) (demo_requests_total) > 1000
            for: 2m
            labels: { severity: warning, scenario: latency_spike }
            annotations: { summary: "downstream latency elevated", description: "request volume anomaly" }
          # --- coverage hand-off close-out (Story 7.3): the 2 prometheus rules 7.2 omitted.
          # Both are PROD-ONLY (eval/scenarios.py A8: disk + memory are trustworthy only
          # multi-node). The demo SUT's single-node service-metric exposition (render_prometheus)
          # does NOT emit node_exporter / cAdvisor resource series, so these rules cannot fire
          # against demo — they complete the detection RULES surface for a prod deploy. The POC
          # proves the chaos→trigger→canonical path IN-PROCESS (tests/test_chaos_inject.py), not
          # via a live fire; memory_leak's non-deterministic tolerance window is Story 6.3.
          - alert: NodeDiskPressure
            expr: (1 - (node_filesystem_avail_bytes{mountpoint="/"} / node_filesystem_size_bytes{mountpoint="/"})) > 0.95
            for: 2m
            labels: { severity: critical, scenario: disk_pressure }
            annotations: { summary: "node disk pressure", description: "node filesystem >95% full, DiskPressure condition" }
          - alert: MemoryUsageHigh
            expr: container_memory_working_set_bytes{container!=""} / container_spec_memory_limit_bytes{container!=""} > 0.9
            for: 2m
            labels: { severity: critical, scenario: memory_leak }
            annotations: { summary: "memory usage high", description: "container working-set >90% of the memory limit" }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: observability
  labels: { app.kubernetes.io/name: prometheus }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: prometheus } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: prometheus }
    spec:
      serviceAccountName: observability-stack
      containers:
        - name: prometheus
          image: prom/prometheus:v2.54.1
          args: [--config.file=/etc/prometheus/prometheus.yml, --storage.tsdb.path=/prometheus]
          ports: [{ containerPort: 9090 }]
          readinessProbe: { httpGet: { path: /-/ready, port: 9090 }, initialDelaySeconds: 5, periodSeconds: 5 }
          resources: { requests: { cpu: 100m, memory: 256Mi }, limits: { cpu: 500m, memory: 512Mi } }
          volumeMounts:
            - { name: config, mountPath: /etc/prometheus, readOnly: true }
            - { name: data, mountPath: /prometheus }
      volumes:
        - name: config
          configMap: { name: prometheus-config }
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: observability
spec:
  selector: { app.kubernetes.io/name: prometheus }
  ports: [{ name: http, port: 9090, targetPort: 9090 }]
