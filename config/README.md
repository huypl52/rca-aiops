# `config/` — Declarative config

Trách nhiệm: floor registry (declarative YAML, AD-12), confidence mapping (AD-7), namespaces (single-tenant `demo` POC).

**Trạng thái:** SKELETON (Story 0.1) — registry thực ở Epic 4/3.
