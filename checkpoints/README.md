# `checkpoints/` — SqliteSaver checkpoint backend (AD-11)

Trách nhiệm: SqliteSaver file backend cho LangGraph checkpoint (POC, AD-11).

**Trạng thái:** DATA DIR (Story 0.1) — placeholder. SqliteSaver wiring + startup dispatcher resume ở Story 7.4.
